# GENUS_CORE Quality Gates v0.3.9

Status: active for v0.4.0 Passive Boundary Relevance Spec

## 86. v0.3.9 Ledger Test Modularization Gate

`v0.3.9 - Ledger Test Modularization` is accepted only if:

```text
pytest green
CLI smoke test green
package version is 0.3.9
SCHEMA_VERSION remains genus.foundation.v0.0.1
tests/test_ledger_lineage_model_hardening.py exists
tests/test_ledger_lineage_sqlite_hardening.py exists
tests/test_ledger_lineage_hardening.py is removed
Ledger lineage tests remain under the normal test line-count target
LedgerEntry target_kind and target_id hardening remains tested
SQLite ledger constraints remain tested
public foundation functions remain unchanged
CLI exposes only observe
SQLite tables remain evidence_records and ledger_entries
allowed sentence types remain unchanged
no runtime capability is added
```

## 87. v0.3.9 Stop Gate

Stop development if any of these occur:

```text
SCHEMA_VERSION changes from genus.foundation.v0.0.1.
A new CLI command appears.
A new SQLite table appears.
A new sentence type appears.
A runtime capability is added.
Ledger target_kind or target_id hardening coverage is lost.
Ledger SQLite constraint coverage is lost.
Worker, LLM, MemoryWrite, Reaction, TransitionCandidate, ConstraintDecision, GraphDB, or RuntimeShape appears as active capability.
```
