# GENUS_CORE Quality Gates v0.4

Status: active for v0.4.2 Passive Boundary Relevance Boundary Audit

## 88. v0.4.0 Passive Boundary Relevance Spec Gate

`v0.4.0 - Passive Boundary Relevance Spec` is accepted only if:

```text
pytest green
CLI smoke test green
package version is 0.4.0
SCHEMA_VERSION remains genus.foundation.v0.0.1
PASSIVE_BOUNDARY_RELEVANCE_SPEC_v0.4.0.md exists
the spec is accepted spec-only
the spec states no runtime implementation is introduced
the spec references ARTIFACT_CONTRACTS.md
future artifacts are named only for later implementation
planned preview shape excludes no_boundary_evaluation_possible
planned report shape excludes no_boundary_evaluation_possible
planned report does not create new lineage
planned artifacts remain ephemeral-only
PassiveBoundaryRelevancePreview != ConstraintDecision
PassiveBoundaryRelevanceReport != PolicyResult
observed_boundary_relevance != permission
boundary_question != allow/block
boundary_area is a closed enum
first boundary_area values are memory_boundary, passive_foundation_boundary, and passive_preview_boundary
unknown boundary_area values must be rejected in a future implementation
observed_boundary_relevance is descriptive-only and non-numeric
observed_boundary_relevance is not level, score, rank, priority, severity, weight, recommendation, or permission
no src/genus_core/passive_boundary* runtime package exists
public foundation functions remain unchanged
CLI exposes only observe
SQLite tables remain evidence_records and ledger_entries
```

## 92. v0.4.2 Passive Boundary Relevance Boundary Audit Gate

`v0.4.2 - Passive Boundary Relevance Boundary Audit` is accepted only if:

```text
pytest green
CLI smoke test green
package version is 0.4.2
SCHEMA_VERSION remains genus.foundation.v0.0.1
no new runtime artifact is added beyond v0.4.1
PassiveBoundaryRelevanceReport summary remains descriptive-only
boundary_question remains neutral and question-like
observed_boundary_relevance remains non-numeric and non-prioritizing
passive_foundation_boundary is still not emitted
report payload mirrors source lineage from PassiveBoundaryRelevancePreview only
runtime field names and report payload keys do not contain evaluation
CLI exposes only observe
SQLite tables remain evidence_records and ledger_entries
```

## 93. v0.4.2 Stop Gate

Stop development if any of these occur:

```text
boundary evaluated appears in runtime summary wording.
policy result or permission granted appears in runtime summary wording.
observed_boundary_relevance becomes numeric, score, rank, priority, severity, or weight.
passive_foundation_boundary is emitted.
evaluation appears as a runtime field or payload key.
A new CLI command appears.
A new SQLite table appears.
A new sentence type appears.
```

## 90. v0.4.1 Passive Boundary Relevance Preview Seed Gate

`v0.4.1 - Passive Boundary Relevance Preview Seed` is accepted only if:

```text
pytest green
CLI smoke test green
package version is 0.4.1
SCHEMA_VERSION remains genus.foundation.v0.0.1
genus_core.passive_boundary_relevance exists
genus_core.functions.__all__ remains unchanged
PassiveBoundaryRelevancePreview and PassiveBoundaryRelevanceReport are ephemeral
build_passive_boundary_relevance_preview reads only BeliefStateSnapshot, PassiveMetricSnapshot, and PassiveTransitionPreview
source_state_id and source_evidence_ids_json must match across all inputs
source_metric_snapshot_id must match the PassiveMetricSnapshot
boundary_area is a closed enum
v0.4.1 emits only memory_boundary and passive_preview_boundary
passive_foundation_boundary remains spec-known but is not emitted
observed_boundary_relevance emits only memory_request_relevance_observed or no_boundary_relevance_observed
boundary_question is question-like and neutral
runtime fields do not include evaluation
payloads reject permission, policy, policy_result, allow, block, approve, reject, recommendation, score, rank, priority, severity, weight, execute, decision, action, reaction, memory_write, and truth
report payload mirrors source lineage from PassiveBoundaryRelevancePreview only
SQLite tables remain evidence_records and ledger_entries
CLI exposes only observe
```

## 91. v0.4.1 Stop Gate

Stop development if any of these occur:

```text
SCHEMA_VERSION changes from genus.foundation.v0.0.1.
PassiveBoundaryRelevancePreview enters genus_core.functions.__all__.
passive_foundation_boundary is emitted.
no_boundary_evaluation_possible appears as a runtime field.
boundary_question recommends, instructs, permits, approves, executes, writes, or invokes policy.
PolicyResult, ConstraintDecision, TransitionCandidate, Reaction, MemoryWrite, Worker, LLM, GraphDB, or RuntimeShape appears.
A new CLI command appears.
A new SQLite table appears.
A new sentence type appears.
```

## 89. v0.4.0 Stop Gate

Stop development if any of these occur:

```text
SCHEMA_VERSION changes from genus.foundation.v0.0.1.
src/genus_core/passive_boundary* appears.
PassiveBoundaryRelevancePreview or PassiveBoundaryRelevanceReport appears as a runtime class.
build_passive_boundary_relevance_preview or create_passive_boundary_relevance_report appears as a runtime function.
no_boundary_evaluation_possible appears as a planned or runtime field.
A new CLI command appears.
A new SQLite table appears.
A new sentence type appears.
The spec permits permission evaluation, policy evaluation, approval, rejection, allow/block, decision, recommendation, priority, score, rank, severity, weight, execute, reaction, memory write, TransitionCandidate, ConstraintDecision, PolicyResult, Worker, LLM, GraphDB, or RuntimeShape.
```
