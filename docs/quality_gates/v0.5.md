# GENUS_CORE Quality Gates v0.5

Status: planned docs-only gates for GENUS_KERNEL STCT baseline.

v0.5.0 is not accepted by this file. It defines the gate that must pass before
the docs-only baseline may be treated as accepted.

## 95. v0.5.0 GENUS_KERNEL STCT Spec Baseline Gate

`v0.5.0 - GENUS_KERNEL STCT Spec Baseline` is accepted only if:

```text
pytest green
CLI smoke test green
package version remains 0.4.3 unless a separate release bump is accepted
SCHEMA_VERSION remains genus.foundation.v0.0.1
GENUS_CORE foundation chain remains unchanged
GENUS_KERNEL remains docs-only
docs/kernel/ contains spec and plan documents only
kernel vocabulary defines future spec names only
kernel vocabulary states that those names are not active runtime artifacts
GENUS_KERNEL must not observe raw input
GENUS_KERNEL consumes BeliefStateSnapshot or explicit future StateInput only
StateInput remains undefined until a separate accepted spec
KernelReport no_action statement is not ConstraintDecision(no_action)
KernelTraceEntry remains spec-level only
KernelTraceEntry does not replace LedgerEntry
KernelTraceEntry does not create a durable truth layer
kernel roadmap keeps CORE and KERNEL below cells and organs
repo placement remains undecided or explicitly docs-only
all new docs stay within normal line limits
```

## 96. v0.5.0 Stop Gate

Stop development if any of these occur:

```text
src/ changes.
tests/ changes without an explicit docs-governance reason.
README.md, STATUS.md, or pyproject.toml changes without release acceptance.
SCHEMA_VERSION changes from genus.foundation.v0.0.1.
A new CLI command appears.
A new SQLite table appears.
KernelStateSnapshot appears as runtime code.
KernelTransitionPreview appears as runtime code.
KernelConstraintCheckPreview appears as runtime code.
KernelTraceEntry appears as runtime code.
KernelReport appears as runtime code.
TransitionCandidate appears.
ConstraintDecision appears.
PolicyResult appears.
allow/block appears as an accepted kernel result.
approval/rejection appears as an accepted kernel result.
MemoryWrite appears.
Reaction appears.
Worker appears.
LLM appears.
Agent, RuntimeCell, Organ, GraphDB, Autonomy, or Self-mutation appears.
```

## Architecture Review Result

Expected v0.5.0 review result:

```text
Philosophy-Fit: green
Governance-Fit: green
Function-Granularity-Fit: green
Overengineering-Risk: low
Documentation-Drift-Risk: medium
Decision: accept only as docs-only baseline
```
