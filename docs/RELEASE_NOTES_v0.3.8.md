# GENUS_CORE v0.3.8 Release Notes

Release: Historical Spec Boundary

Status: governance/documentation release; no new runtime capability

## Summary

`GENUS_CORE v0.3.8` marks historical v0.0.1 specs as frozen references and
adds `docs/SPEC_BOUNDARIES.md`.

Active build authority remains in current governance documents, not in
historical specs.

## Boundaries

No runtime source package, CLI command, SQLite table, sentence type,
`SCHEMA_VERSION` change, worker, LLM, memory write, reaction, transition
candidate, constraint decision, GraphDB, or RuntimeShape is introduced.
